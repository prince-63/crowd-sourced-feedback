class VoteResponseDTO {
    constructor(voteId, userId, productId) {
        this.voteId = voteId;
        this.userId = userId;
        this.productId = productId;
    }
}

export default VoteResponseDTO;